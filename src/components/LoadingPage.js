import React from "react";

const LoadingPage = () => ( //JSX in here, use image tags for gifs
    <div className='loader'>
        <img className='loader__image' src='/images/loader.gif' /> 
    </div>
);

export default LoadingPage;